#!/bin/bash

DIR=${1:-$HOME/bin}

mkdir -p "$DIR"

for script in "$(dirname "$0")"/*.py; do
    [[ -f $script ]] || continue
    cp -fv "$script" "$DIR/$(basename "$script" .py)"
done

echo "All scripts should be installed. Be sure '$DIR' is in your PATH." |
sed "s%${HOME//%/\%}%~%g"
